Bag-of-Words (CBOW) and Skip-Gram, make use
of shallow neural network architectures to either predict a word given a context window (the former), or predict the
context window for a given word (the later)

One of the most recent and popular word embedding generation methods is Glove presented in [27]. Word2vec


Raw Text
   ↓
Data Preparation
   ↓
Vectorization / Tokenization
   ↓
Embedding
   ↓
GRU or RNN Model Training
   ↓
Evaluation
   ↓
Prediction



Vectorization
    ├── Traditional vectorization
    │      ├── Bag-of-Words (BOW)
    │      └── TF-IDF
    │
    └── Embedding-based vectorization
           ├── Word2Vec
           ├── GloVe
           ├── FastText
           └── tf.keras.layers.Embedding
		   
The Article talked about the Vectorization and about the Transitional methods like Bag-of-Words and TF-IDF That are used by in Natural Language Processing (NLP). Also talked about modern embedding methods (part of vectoization) like Word2Vec and GloVe.
