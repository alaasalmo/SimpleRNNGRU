# TensorFlow CPU build
tensorflow-cpu==2.15.0

# Metrics
scikit-learn==1.4.2

# Pin numpy to a version compatible with TF 2.15
numpy==1.26.4

# NOTE: 'datasets' (HuggingFace) is NOT needed here —
# this script reads all-data.csv directly via Python's csv module.
