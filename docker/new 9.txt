from keras import regularizers 
from keras.datasets import imdb 
(train_data, train_labels), (test_data, test_labels) = imdb.load_data(num_words=10000) 

def vectorize_sequences(sequences, dimension=10000): 
    # create an all-zero matrix of shape (len(sequences), dimension) 
    results = np.zeros((len(sequences), dimension)) 
    for i, sequence in enumerate(sequences): 
        results[i, sequence] = 1.0 # set specific indices of results[i] to 1s 
    return results
	

x_train = vectorize_sequences(train_data) # vectorized training data 
x_test = vectorize_sequences(test_data) # vectorized test data 
y_train = np.asarray(train_labels, dtype = 'float32') # vectorize labels 
y_test = np.asarray(test_labels, dtype = 'float32')


# create a validation set from training set 
x_val = x_train[:10000] 
partial_x_train = x_train[10000:] 
y_val = y_train[:10000] 
partial_y_train = y_train[10000:]

1. Train a model for text classification over an IMDB dataset, perform experiments with the 
above created validation loss, and plot the validation loss against a number of epochs. 
Configurations: 
model1: 3 hidden layers, 16 hidden units in each layer 
optimizer='rmsprop', loss='binary_crossentropy', metrics=['accuracy'] 
epochs=20, batch_size = 512


history = model1.fit(
    partial_x_train,
    partial_y_train,
    epochs=20,
    batch_size=512,
    validation_data=(x_val, y_val)
)

import matplotlib.pyplot as plt

val_loss = history.history['val_loss']
epochs = range(1, len(val_loss) + 1)

plt.plot(epochs, val_loss)
plt.xlabel('Epochs')
plt.ylabel('Validation Loss')
plt.title('Validation Loss vs Epochs (IMDB – Model 1)')
plt.show()

