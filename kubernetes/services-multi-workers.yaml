apiVersion: v1
kind: Service
metadata:
  name: bitsimplernn-worker-0
spec:
  clusterIP: None          # headless, so DNS resolves straight to the pod
  selector:
    app: bisimplernn-worker
    worker-index: "0"
  ports:
    - port: 12345
      targetPort: 12345
---
apiVersion: v1
kind: Service
metadata:
  name: bitsimplernn-worker-1
spec:
  clusterIP: None
  selector:
    app: bisimplernn-worker
    worker-index: "1"
  ports:
    - port: 12345
      targetPort: 12345
---
apiVersion: v1
kind: Service
metadata:
  name: bitsimplernn-worker-2
spec:
  clusterIP: None
  selector:
    app: bisimplernn-worker
    worker-index: "2"
  ports:
    - port: 12345
      targetPort: 12345
